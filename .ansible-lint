exclude_paths:
    - "**/.github/**"
    - .ansible-lint
    - "**/.ansible/**"
    - "**/molecule/**"     # exclude any molecule dir anywhere
    - "**/meta/**"         # exclude any meta dir anywhere
    - galaxy.yml
    - "**/changelogs/**"
    - devfile.yaml

mock_modules:
  - ansible.posix.mount
