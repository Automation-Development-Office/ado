# Native build deps for Hub collection Python requirements (e.g. systemd-python).
# Match ansible.eda bindep profile names (debian/rpm, not dpkg).
pkg-config [compile]
gcc [compile]
libsystemd0 [platform:debian]
libsystemd-dev [compile platform:debian]
systemd-devel [compile platform:rpm]
