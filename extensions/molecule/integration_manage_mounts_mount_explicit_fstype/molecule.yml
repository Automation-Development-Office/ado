---
platforms:
  - name: na

provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: "${MOLECULE_SCENARIO_DIRECTORY}/../../../roles:${ANSIBLE_ROLES_PATH}"
  playbooks:
    converge: ../utils/playbooks/manage_mounts_converge_mount_explicit_fstype.yml
    destroy: ../utils/playbooks/manage_mounts_destroy_mount_explicit_fstype.yml
    verify: ../utils/playbooks/manage_mounts_verify_mount_explicit_fstype.yml
    prepare: ../utils/playbooks/manage_mounts_prepare_mount_explicit_fstype.yml
  config_options:
    defaults:
      collections_path: ${ANSIBLE_COLLECTIONS_PATH}

scenario:
  test_sequence:
    - prepare
    - converge
    - idempotence
    - verify
  destroy_sequence:
    - destroy
