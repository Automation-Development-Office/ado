---
- name: Prepare - grafana_manage_folders scenario
  hosts: localhost
  gather_facts: false
  become: false

  tasks:
    - name: Prepare step is intentionally minimal
      ansible.builtin.debug:
        msg: "No explicit prepare actions required for grafana_manage_folders."
