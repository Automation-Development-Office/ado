---
- name: Converge - rhel_repos RHSM scenario
  hosts: localhost
  gather_facts: true
  # become: true
  # tasks:
  #   - name: Exercise RHSM repository flow
  #     block:
  #       - name: Run role with rhsm_repository method
  #         ansible.builtin.include_role:
  #           name: infra.ado.rhel_repos
  #         vars: # noqa: var-naming[no-role-prefix]
  #           repos_action: "enable"
  #           repos_to_enable: "rhel-8-for-x86_64-baseos-rpms"
  #           repos_enable_method: "rhsm_repository"
  #           rhel_repos_clean_cache: false
  #     rescue:
  #       - name: RHSM test completed with expected limitations in container
  #         ansible.builtin.debug:
  #           msg: "RHSM operation failed as expected in unregistered container; scenario continues."
