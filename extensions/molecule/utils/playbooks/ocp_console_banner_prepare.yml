---
- name: Prepare - ocp_console_banner scenario
  hosts: localhost
  gather_facts: false
  become: false

  tasks:
    - name: Prepare step is intentionally minimal
      ansible.builtin.debug:
        msg: "No explicit prepare actions required for ocp_console_banner."
