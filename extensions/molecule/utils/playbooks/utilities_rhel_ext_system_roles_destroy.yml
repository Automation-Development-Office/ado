---
- name: Destroy - utilities_rhel_ext_system_roles scenario
  hosts: localhost
  gather_facts: false
  become: false

  tasks:
    - name: Destroy step is intentionally minimal
      ansible.builtin.debug:
        msg: "No explicit destroy actions required for utilities_rhel_ext_system_roles."
