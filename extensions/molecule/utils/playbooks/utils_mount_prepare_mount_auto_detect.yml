---
- name: Prepare - utils_mount mount auto-detect scenario
  hosts: localhost
  gather_facts: true
  become: true

  vars:
    utils_mount_test_root: /tmp/utils_mount_molecule_mount_auto_detect
    utils_mount_image_path: "{{ utils_mount_test_root }}/device.img"
    utils_mount_mount_path: "{{ utils_mount_test_root }}/mnt"

  tasks:
    - name: Ensure Debian package dependencies are present
      ansible.builtin.apt:
        name:
          - e2fsprogs
          - util-linux
        state: present
        update_cache: true
      when: ansible_facts["os_family"] == "Debian"

    - name: Ensure scenario test root exists
      ansible.builtin.file:
        path: "{{ utils_mount_test_root }}"
        state: directory
        mode: "0755"

    - name: Ensure scenario mount path exists
      ansible.builtin.file:
        path: "{{ utils_mount_mount_path }}"
        state: directory
        mode: "0755"

    - name: Ensure loopback image exists
      ansible.builtin.command: "truncate -s 64M {{ utils_mount_image_path }}"
      args:
        creates: "{{ utils_mount_image_path }}"

    - name: Detect existing filesystem signature in loopback image
      ansible.builtin.command: "blkid {{ utils_mount_image_path }}"
      register: utils_mount_prepare_blkid_result
      changed_when: false
      failed_when: false

    - name: Create ext4 filesystem in loopback image when missing
      ansible.builtin.command: "mkfs.ext4 -F {{ utils_mount_image_path }}"
      when: utils_mount_prepare_blkid_result.rc != 0
      changed_when: true

    - name: Ensure test mount path is absent from fstab and unmounted
      ansible.posix.mount:
        path: "{{ utils_mount_mount_path }}"
        state: absent
