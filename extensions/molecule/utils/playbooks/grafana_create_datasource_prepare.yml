---
- name: Prepare - grafana_create_datasource scenario
  hosts: localhost
  gather_facts: false
  become: false

  tasks:
    - name: Prepare step is intentionally minimal
      ansible.builtin.debug:
        msg: "No explicit prepare actions required for grafana_create_datasource."
