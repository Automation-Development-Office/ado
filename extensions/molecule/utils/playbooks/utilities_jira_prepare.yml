---
- name: Prepare - utilities_jira scenario
  hosts: localhost
  gather_facts: false
  become: false

  tasks:
    - name: Prepare - utilities_jira scenario
      ansible.builtin.debug:
        msg: "No explicit prepare actions required for utilities_jira."
