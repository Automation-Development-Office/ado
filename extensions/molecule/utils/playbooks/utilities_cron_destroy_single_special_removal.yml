---
- name: Destroy - utilities_cron single special removal scenario
  hosts: localhost
  gather_facts: false
  become: true

  tasks:
    - name: Ensure yearly test cron job is absent at destroy
      ansible.builtin.cron:
        name: yearly_test
        state: absent
