---
- name: Prepare - utilities_services_management scenario
  hosts: localhost
  gather_facts: false
  become: false

  tasks:
    - name: Prepare step is intentionally minimal
      ansible.builtin.debug:
        msg: "No explicit prepare actions required for utilities_services_management."
