---
platforms:
  - name: na

provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: "${MOLECULE_SCENARIO_DIRECTORY}/../../../roles:${ANSIBLE_ROLES_PATH}"
  playbooks:
    prepare: ../utils/playbooks/applications_grafana_create_datasource_prepare.yml
    converge: ../utils/playbooks/applications_grafana_create_datasource_converge.yml
    verify: ../utils/playbooks/applications_grafana_create_datasource_verify.yml
    destroy: ../utils/playbooks/applications_grafana_create_datasource_destroy.yml
  config_options:
    defaults:
      collections_path: ${ANSIBLE_COLLECTIONS_PATH}

scenario:
  test_sequence:
    - prepare
    - converge
    - idempotence
    - verify
  destroy_sequence:
    - destroy
