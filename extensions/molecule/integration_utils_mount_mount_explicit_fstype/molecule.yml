---
platforms:
  - name: na

provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: "${MOLECULE_SCENARIO_DIRECTORY}/../../../roles:${ANSIBLE_ROLES_PATH}"
  playbooks:
    converge: ../utils/playbooks/utils_mount_converge_mount_explicit_fstype.yml
    destroy: ../utils/playbooks/utils_mount_destroy_mount_explicit_fstype.yml
    verify: ../utils/playbooks/utils_mount_verify_mount_explicit_fstype.yml
    prepare: ../utils/playbooks/utils_mount_prepare_mount_explicit_fstype.yml
  config_options:
    defaults:
      collections_path: ${ANSIBLE_COLLECTIONS_PATH}

scenario:
  test_sequence:
    - prepare
    - converge
    - idempotence
    - verify
  destroy_sequence:
    - destroy
