# Scenarios excluded from automatic Molecule runs on pull_request.
#
# One scenario name or shell glob per line. Lines starting with # are ignored.
# Matched scenarios are skipped in PR CI but can still be run locally or via
# workflow_dispatch (enable "Run OpenShift (ocp_*) scenarios").
#
# OpenShift scenarios require K8S_AUTH_* credentials and a reachable cluster.
ocp_*
install_aap
install_dirsrv
install_elastic
install_postfix
install_rhbk
integration_rhel_repos_rhsm
