---
platforms:
  - name: na

provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: "${MOLECULE_SCENARIO_DIRECTORY}/../../../roles:${ANSIBLE_ROLES_PATH}"
  playbooks:
    converge: ../utils/playbooks/manage_mounts_converge_mount_auto_detect.yml
    destroy: ../utils/playbooks/manage_mounts_destroy_mount_auto_detect.yml
    verify: ../utils/playbooks/manage_mounts_verify_mount_auto_detect.yml
    prepare: ../utils/playbooks/manage_mounts_prepare_mount_auto_detect.yml
  config_options:
    defaults:
      collections_path: ${ANSIBLE_COLLECTIONS_PATH}

scenario:
  test_sequence:
    - prepare
    - converge
    - idempotence
    - verify
  destroy_sequence:
    - destroy
