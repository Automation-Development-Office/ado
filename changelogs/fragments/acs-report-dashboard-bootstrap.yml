---
minor_changes:
  - acs_report - Ship a Grafana posture dashboard with Red Hat-source and raw
    component bar charts, unique CVE / Critical / Important totals, daily
    discovery inflow, and cumulative backlog growth.
  - bootstrap_controller - Add RHACS report job templates (survey, rhsource,
    raw, age) and ``ADO | ACS RHACS Report Workflow`` when the ``acs_report``
    option is selected.
  - bootstrap_generate_env_vars - Expand ACS ``component_options`` into
    playbook/JT selectors (same pattern as Satellite/IdM).
  - preflight - ACS form option to deploy RHACS vulnerability report job
    templates and workflow.
