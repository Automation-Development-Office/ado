---
bugfixes:
  - >-
    bootstrap_generate_playbook_repo - Fixed generated bootstrap seed playbook
    YAML indentation and malformed ``vars_files`` entries so generated
    playbooks pass syntax and lint checks.
  - >-
    bootstrap_generate_playbook_repo - Fixed generated bootstrap seed
    playbooks to reference real role names for ``include_role`` tasks.
  - >-
    ocp_operatorgroups - Fixed task imports to reference existing
    ``create_operatorgroup.yml`` and ``delete_operatorgroup.yml`` task files.
minor_changes:
  - >-
    ci - Added ADO collection roles to ``mock_roles`` and excluded generated
    bootstrap seed payloads from source lint checks.
