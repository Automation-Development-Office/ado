---
minor_changes:
  - >-
    Add Satellite install sizing, storage mount, deployment version, RHN
    organization, location, admin password, and activation key defaults to the
    bootstrap env var generator so UI and CLI preflight runs can populate the
    Satellite install role.
  - >-
    Add default Satellite install tuning tiers and storage mount definitions to
    the Satellite install role.
