---
bugfixes:
  - >-
    aap_ocp_install, aap_ocp_install_upstream, ocp_iscsi_storage - align role
    README headings with the collection README format template so README
    verification passes.
  - >-
    ocp_iscsi_storage - wrap multi-document CSI manifests as Kubernetes List
    resources and clear executable bits so ansible-test yamllint and shebang
    sanity checks pass.
