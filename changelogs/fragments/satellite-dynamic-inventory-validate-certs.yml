---
bugfixes:
  - bootstrap_generate_env_vars - render the Satellite TLS validation setting
    into dynamic inventory source vars so AAP Satellite inventory syncs honor
    the UI and CLI skip TLS setting.
