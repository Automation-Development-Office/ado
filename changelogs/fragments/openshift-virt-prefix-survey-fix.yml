---
bugfixes:
  - >-
    bootstrap_controller - fix OpenShift Virt VM survey apply failure where
    provision_openshift_virt_prefix_length default was emitted as the string
    "24" instead of integer 24 (quoted placeholder substitution order).
minor_changes:
  - >-
    OpenShift Virt network defaults - drop preflight static IP seeding; add
    optional ip_range guidance for the survey help text while keeping gateway,
    DNS, and prefix_length as optional survey defaults.
