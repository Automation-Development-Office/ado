---
bugfixes:
  - |
    ci - Stop shipping local ``__pycache__``, ansible-lint ``.cache``, and
    ``.ansible-tmp`` artifacts so ansible-test sanity extra-files checks pass.
    Collection CI helpers under ``scripts/`` stay as Python (not Ansible).
