---
bugfixes:
  - >-
    ocp_zabbix - DB init Job loads ``create.sql.gz`` from current ubuntu
    server images (old ``server.sql.gz`` split is gone); delete failed Job
    before recreate.
  - >-
    ocp_quay - Bypass RHEL Postgres entrypoint ``chmod``/``chown`` that
    CrashLoops on Synology NFS root_squash; keep anyuid + initContainer.
  - >-
    ocp_acs - Ensure ``central-pvc`` / ``central-db`` / ``central-db-backup``
    PVCs exist before Central (claimName does not auto-create).
  - >-
    aap_ocp_install - Force ``aap_ocp_install_reset_database=true`` in the
    bootstrap playbook (Contoller vars left it false so Fernet reset skipped).
  - >-
    ocp_operator_subscription / dev_hub - Delete stale
    ``redhat-developer-hub-operator-sub`` so OLM can resolve package ``rhdh``.
