---
bugfixes:
  - >-
    Install native build dependencies before tox ade install so sanity and unit
    jobs can resolve Hub collection Python requirements such as systemd-python.
  - >-
    Fix bindep platform profiles and always install libsystemd-dev and
    libkrb5-dev before ade pip requirements (avoids empty bindep match skipping
    the apt fallback; gssapi from ansible.eda needs krb5-config).
