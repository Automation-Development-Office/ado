---
minor_changes:
  - acs_report - New role to generate RHACS vulnerability reports from Central
    workload export (``raw`` / ``rhsource`` / ``age`` / ``all``), with
    ``--all`` / ``--rhsre`` / ``--sev`` / ``--component`` scope options and
    companion summary JSON.
  - bootstrap - Seed playbook ``playbooks/acs/ado-acs-report.yml`` and Controller
    JT ``ADO | ACS RHACS Vulnerability Report``.
  - acs_report - Ship sample Grafana dashboard
    ``files/grafana/rhacs-vulnerability-overview.json`` seeded from scrubbed
    prod Critical+Important+Fixable component counts.
