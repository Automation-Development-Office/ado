---
bugfixes:
  - >-
    bootstrap_generate_playbook_repo - Fail when Hub mode leaves vendored
    infra.ado in git or requirements.yml still uses type:dir; skip metadata
    stamp when Hub mode is on.
  - >-
    bootstrap_controller - Hub-only publish no longer touches git or Controller
    project sync; requirements.yml and vendored-tree cleanup happen only on
    full bootstrap (generate_playbooks).
