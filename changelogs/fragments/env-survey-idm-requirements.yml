---
bugfixes:
  - bootstrap_generate_env_vars - Emit survey ``environment_choices`` from the primary
    preflight environment plus optional ``additional_environments`` (no hardcoded
    ``dev/test/preprod/prod`` list when generating from preflight).
  - bootstrap_controller - Prefer generated ``controller_bootstrap_environment_choices``
    over the old fixed survey list.
minor_changes:
  - collections - Declare ``redhat.rhel_idm`` in collection requirements and playbook
    repo seed/template so IdM server/client/replica jobs can install the certified
    collection from Automation Hub.
  - infra.ado.idm_ad_trust - Update default AD forest targets to ``ad.lab`` /
    ``adwindows.ad.lab`` / ``192.168.0.61``.
