---
minor_changes:
  - >-
    Add RHEL patching survey prompts for reboot behavior, package selection,
    package state, exclusions, disabled repositories, cache refresh, kernel
    cleanup, and skip-broken handling.
  - >-
    Normalize RHEL patching list inputs so AAP survey text answers can be used
    as package, exclude, and disabled repository lists.
  - >-
    Correct the Satellite registration job template survey definition to use
    survey_spec and the standard bootstrap controller variables.
