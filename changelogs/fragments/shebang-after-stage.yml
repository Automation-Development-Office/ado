---
bugfixes:
  - |
    acs_report - Keep ``rhacs-report.sh`` without a shebang in git (mode 0644)
    so ansible-test shebang sanity passes. After copy to ``/tmp``, the role
    inserts ``#!/bin/bash`` and sets mode 0755.
  - |
    grafana_upload_dashboards - Drop ``#!/usr/bin/env python3`` from
    ``adapt-dashboard-for-folder.py``. The task already runs it with
    ``python3``, so a collection shebang is not required.
