---
- name: Validate controller_credentials is defined
  ansible.builtin.assert:
    that:
      - controller_credentials is defined
      - controller_credentials | length > 0
    fail_msg: "controller_credentials is not defined or empty"

- name: Apply credentials
  ansible.builtin.include_role:
    name: ansible.controller.controller_credentials
