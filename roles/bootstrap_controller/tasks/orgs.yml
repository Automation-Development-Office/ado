---
- name: Validate controller_organizations is defined
  ansible.builtin.assert:
    that:
      - controller_organizations is defined
      - controller_organizations | length > 0
    fail_msg: "controller_organizations is not defined or empty"

- name: Apply organizations
  ansible.builtin.include_role:
    name: ansible.controller.controller_organizations
