---
- name: Validate controller_hosts is defined
  ansible.builtin.assert:
    that:
      - controller_hosts is defined
      - controller_hosts | length > 0
    fail_msg: "controller_hosts is not defined or empty"

- name: Apply hosts
  ansible.builtin.include_role:
    name: ansible.controller.controller_hosts
