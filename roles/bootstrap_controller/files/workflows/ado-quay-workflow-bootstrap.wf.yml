---
controller_workflow_job_templates:
  - name: ADO | Quay Workflow
    description: >-
      Quay on OpenShift — install (or configure) then optional auth step
      based on preflight Quay options.
    organization: "{{ bootstrap_controller_organization }}"
    inventory: "{{ bootstrap_controller_inventory_name }}"
    state: present
    destroy_current_nodes: true
    labels:
      - ADO | bootstrap
      - ADO | quay
    simplified_workflow_nodes:
      - identifier: Deploy Quay
        unified_job_template: ADO | Deploy Quay
        lookup_organization: "{{ bootstrap_controller_organization }}"
        success_nodes:
          - Deploy Quay OIDC

      - identifier: Deploy Quay OIDC
        unified_job_template: ADO | Deploy Quay OIDC
        lookup_organization: "{{ bootstrap_controller_organization }}"
