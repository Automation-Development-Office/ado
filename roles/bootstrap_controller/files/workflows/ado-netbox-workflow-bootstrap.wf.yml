---
controller_workflow_job_templates:
  - name: ADO | NetBox Workflow
    description: >-
      NetBox OIDC configuration on OpenShift when selected in preflight.
    organization: "{{ bootstrap_controller_organization }}"
    inventory: "{{ bootstrap_controller_inventory_name }}"
    state: present
    destroy_current_nodes: true
    labels:
      - ADO | bootstrap
      - ADO | netbox
    simplified_workflow_nodes:
      - identifier: Configure NetBox OIDC
        unified_job_template: ADO | Configure NetBox OIDC
        lookup_organization: "{{ bootstrap_controller_organization }}"
