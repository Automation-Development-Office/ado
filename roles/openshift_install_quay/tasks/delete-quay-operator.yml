---
- name: Delete Quay namespace
  kubernetes.core.k8s:
    api_version: v1
    kind: Namespace
    name: "{{ name_space }}"
    state: absent
  register: openshift_install_quay_delete_namespace
  failed_when: >
    openshift_install_quay_delete_namespace.failed and (
      'NotFound' not in (openshift_install_quay_delete_namespace | to_json)
      and 'not found' not in (openshift_install_quay_delete_namespace | to_json)
    )
