---
bootstrap_generate_playbook_repo_dest: "{{ playbook_dir }}"
bootstrap_generate_playbook_repo_seed_src: >-
  {{ role_path }}/files/playbook_repo_seed
bootstrap_generate_playbook_repo_force: true

bootstrap_generate_playbook_repo_git_mode: manual
bootstrap_generate_playbook_repo_git_remote: origin
bootstrap_generate_playbook_repo_git_branch: main
bootstrap_generate_playbook_repo_git_message: "Generate bootstrap playbook repo"
bootstrap_generate_playbook_repo_git_username: oauth2
bootstrap_generate_playbook_repo_git_token: ""
bootstrap_generate_playbook_repo_git_sync_before_push: true
bootstrap_generate_playbook_repo_pause_after_generate: false

# When false (default), do not write Galaxy/Hub collections/requirements.yml.
# Contoller project sync then skips install_collections. Set true only when
# Hub publish/Galaxy is actually requested.
bootstrap_generate_playbook_repo_write_galaxy_requirements: false

# Leave empty so generated collections/requirements.yml (Hub path only) has
# no version pin. Set only when you must pin.
bootstrap_generate_playbook_repo_infra_ado_collection_version: ""

bootstrap_generate_playbook_repo_component: all

bootstrap_generate_playbook_repo_component_map:
  all:
    - all

  openshift:
    - aap
    - acs
    - acm
    - cert_manager
    - console
    - devspaces
    - dirsrv
    - eck
    - gitops
    - gitlab
    - grafana
    - bookstack
    - kafka
    - oadp
    - openshift
    - pega
    - quay
    - rhbk

  rhel:
    - rhel
    - satellite_server_install
    - satellite_client_tools
    - satellite_content_view
    - satellite_capsule_install
    - satellite_oidc
    - idm_server_install
    - idm_replica_install
    - idm_client_tools
    - idm_dns_install
    - aap
    - dirsrv
    - eck
    - gitlab
    - grafana
    - kafka
    - rhbk
    - compliance
    - stig

  patching:
    - rhel
    - satellite_server_install
    - satellite_client_tools
    - satellite_content_view
    - satellite_capsule_install
    - satellite_oidc
    - idm_server_install
    - idm_replica_install
    - idm_client_tools
    - idm_dns_install

  provision:
    - openshift_virt

  openshift_virt:
    - openshift_virt

  aap:
    - aap

  rhbk:
    - rhbk
    - rhbk_standalone

  rhbk_standalone:
    - rhbk_standalone

  grafana:
    - grafana
    - grafana_standalone

  grafana_standalone:
    - grafana_standalone

  bookstack:
    - bookstack

  netbox:
    - netbox

  kafka:
    - kafka

  gitlab:
    - gitlab
    - gitlab_standalone

  gitlab_standalone:
    - gitlab_standalone

  satellite:
    - satellite_server_install
    - satellite_client_tools
    - satellite_content_view
    - satellite_capsule_install

  idm:
    - idm_server_install
    - idm_replica_install
    - idm_client_tools
    - idm_dns_install

  satellite_server_install:
    - satellite_server_install

  satellite_client_tools:
    - satellite_client_tools

  satellite_content_view:
    - satellite_content_view

  satellite_capsule_install:
    - satellite_capsule_install

  satellite_oidc:
    - satellite_oidc

  idm_server_install:
    - idm_server_install

  idm_replica_install:
    - idm_replica_install

  idm_client_tools:
    - idm_client_tools

  idm_dns_install:
    - idm_dns_install

  rhel-patching:
    - rhel

  stig:
    - stig

  rhel-stig:
    - stig

  compliance:
    - compliance

  pega:
    - pega

  elastic:
    - eck

bootstrap_generate_playbook_repo_generated_playbooks:

  - src: aap/ado-aap-ocp-install-bootstrap.yml
    dest: playbooks/aap/ado-aap-ocp-install-bootstrap.yml
    target_platform: openshift
    category: platform
    app: aap

  - src: acs/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/acs/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: platform
    app: acs

  - src: acs/ado-acs-report.yml
    dest: playbooks/acs/ado-acs-report.yml
    target_platform: openshift
    category: platform
    app: acs

  - src: acm/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/acm/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: platform
    app: acm

  - src: cert-manager/ado-configure-idm-acme-clusterissuer.yml
    dest: playbooks/cert-manager/ado-configure-idm-acme-clusterissuer.yml
    target_platform: openshift
    category: platform
    app: cert_manager

  - src: cert-manager/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/cert-manager/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: platform
    app: cert_manager

  - src: cert-manager/ado-install-and-configure-awspca-bootstrap.yml
    dest: playbooks/cert-manager/ado-install-and-configure-awspca-bootstrap.yml
    target_platform: openshift
    category: platform
    app: cert_manager

  - src: openshift/ado-configure-console-banner-bootstrap.yml
    dest: playbooks/openshift/ado-configure-console-banner-bootstrap.yml
    target_platform: openshift
    category: platform
    app: console

  - src: devspaces/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/devspaces/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: applications
    app: devspaces

  - src: dirsrv/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/dirsrv/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: applications
    app: dirsrv

  - src: dirsrv/ado-ldap-modify-bootstrap.yml
    dest: playbooks/dirsrv/ado-ldap-modify-bootstrap.yml
    target_platform: openshift
    category: applications
    app: dirsrv

  - src: dirsrv/ado-search-users-groups-bootstrap.yml
    dest: playbooks/dirsrv/ado-search-users-groups-bootstrap.yml
    target_platform: openshift
    category: applications
    app: dirsrv

  - src: eck/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/eck/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: applications
    app: eck

  - src: gitlab/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/gitlab/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: applications
    app: gitlab

  - src: gitlab/ado-install-gitlab-standalone-bootstrap.yml
    dest: playbooks/gitlab/ado-install-gitlab-standalone-bootstrap.yml
    target_platform: linux
    category: applications
    app: gitlab_standalone

  - src: gitops/ado-deploy-bootstrap.yml
    dest: playbooks/gitops/ado-deploy-bootstrap.yml
    target_platform: openshift
    category: platform
    app: gitops

  - src: grafana/ado-create-custom-route.yml
    dest: playbooks/grafana/ado-create-custom-route.yml
    target_platform: openshift
    category: platform
    app: grafana

  - src: grafana/ado-deploy-bootstrap.yml
    dest: playbooks/grafana/ado-deploy-bootstrap.yml
    target_platform: openshift
    category: platform
    app: grafana

  - src: grafana/ado-deploy-dashboards-bootstrap.yml
    dest: playbooks/grafana/ado-deploy-dashboards-bootstrap.yml
    target_platform: openshift
    category: platform
    app: grafana

  - src: grafana/ado-install-grafana-standalone-bootstrap.yml
    dest: playbooks/grafana/ado-install-grafana-standalone-bootstrap.yml
    target_platform: linux
    category: platform
    app: grafana_standalone

  - src: docs/ado-deploy-bookstack-bootstrap.yml
    dest: playbooks/docs/ado-deploy-bookstack-bootstrap.yml
    target_platform: openshift
    category: applications
    app: bookstack

  - src: netbox/ado-configure-netbox-oidc-bootstrap.yml
    dest: playbooks/netbox/ado-configure-netbox-oidc-bootstrap.yml
    target_platform: openshift
    category: applications
    app: netbox

  - src: idm/ado-install-replica-bootstrap.yml
    dest: playbooks/idm/ado-install-replica-bootstrap.yml
    target_platform: linux
    category: patching
    app: idm_replica_install

  - src: idm/ado-install-server-bootstrap.yml
    dest: playbooks/idm/ado-install-server-bootstrap.yml
    target_platform: linux
    category: patching
    app: idm_server_install

  - src: idm/ado-manage-client-bootstrap.yml
    dest: playbooks/idm/ado-manage-client-bootstrap.yml
    target_platform: linux
    category: patching
    app: idm_client_tools

  - src: idm/ado-manage-dns-bootstrap.yml
    dest: playbooks/idm/ado-manage-dns-bootstrap.yml
    target_platform: linux
    category: patching
    app: idm_dns_install

  - src: idm/ado-manage-server-settings-bootstrap.yml
    dest: playbooks/idm/ado-manage-server-settings-bootstrap.yml
    target_platform: linux
    category: patching
    app: idm_server_install

  - src: idm/ado-manage-sudo-bootstrap.yml
    dest: playbooks/idm/ado-manage-sudo-bootstrap.yml
    target_platform: linux
    category: patching
    app: idm_server_install

  - src: idm/ado-manage-ad-trust-bootstrap.yml
    dest: playbooks/idm/ado-manage-ad-trust-bootstrap.yml
    target_platform: linux
    category: patching
    app: idm_ad_trust_install

  - src: idm/ado-manage-topology-bootstrap.yml
    dest: playbooks/idm/ado-manage-topology-bootstrap.yml
    target_platform: linux
    category: patching
    app: idm_server_install

  - src: jira/ado-create-stories-bootstrap.yml
    dest: playbooks/jira/ado-create-stories-bootstrap.yml
    target_platform: all
    category: utilities
    app: jira

  - src: kafka/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/kafka/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: applications
    app: kafka

  - src: kafka/ado-setup-alerting-bootstrap.yml
    dest: playbooks/kafka/ado-setup-alerting-bootstrap.yml
    target_platform: openshift
    category: applications
    app: kafka

  - src: oadp/ado-deploy-bootstrap.yml
    dest: playbooks/oadp/ado-deploy-bootstrap.yml
    target_platform: openshift
    category: platform
    app: oadp

  - src: openshift/ado-configure-ldap-auth-bootstrap.yml
    dest: playbooks/openshift/ado-configure-ldap-auth-bootstrap.yml
    target_platform: openshift
    category: platform
    app: openshift_ldap_auth

  - src: openshift/ado-configure-oauth-with-rhbk-bootstrap.yml
    dest: playbooks/openshift/ado-configure-oauth-with-rhbk-bootstrap.yml
    target_platform: openshift
    category: platform
    app: openshift_oauth_rhbk

  - src: openshift/ado-create-htpass-user-bootstrap.yml
    dest: playbooks/openshift/ado-create-htpass-user-bootstrap.yml
    target_platform: openshift
    category: platform
    app: admin_htpasswd

  - src: vpn/ado-manage-vpn-access-bootstrap.yml
    dest: playbooks/vpn/ado-manage-vpn-access-bootstrap.yml
    target_platform: localhost
    category: platform
    app: vpn_access

  - src: openshift/ado-install-nfs-csi-bootstrap.yml
    dest: playbooks/openshift/ado-install-nfs-csi-bootstrap.yml
    target_platform: openshift
    category: platform
    app: nfs_csi

  - src: openshift/ado-install-iscsi-csi-bootstrap.yml
    dest: playbooks/openshift/ado-install-iscsi-csi-bootstrap.yml
    target_platform: openshift
    category: platform
    app: iscsi_csi

  - src: openshift/ado-discover-routes-bootstrap.yml
    dest: playbooks/openshift/ado-discover-routes-bootstrap.yml
    target_platform: openshift
    category: platform
    app: openshift_discover_routes_print

  - src: openshift/ado-discover-routes-bootstrap.yml
    dest: playbooks/openshift/ado-discover-routes-bootstrap.yml
    target_platform: openshift
    category: platform
    app: openshift_discover_routes_alt

  - src: openshift/ado-update-pull-secret-bootstrap.yml
    dest: playbooks/openshift/ado-update-pull-secret-bootstrap.yml
    target_platform: openshift
    category: platform
    app: openshift_update_pull_secret

  - src: provision/ado-provision-openshift-virt-vm-bootstrap.yml
    dest: playbooks/provision/ado-provision-openshift-virt-vm-bootstrap.yml
    target_platform: openshift
    category: provision
    app: openshift_virt

  - src: pega/ado-install-and-configure-bootstrap.yml
    dest: playbooks/pega/ado-install-and-configure-bootstrap.yml
    target_platform: openshift
    category: applications
    app: pega

  - src: quay/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/quay/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: applications
    app: quay

  - src: rhbk/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/rhbk/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: platform
    app: rhbk

  - src: rhbk/ado-install-rhbk-standalone-bootstrap.yml
    dest: playbooks/rhbk/ado-install-rhbk-standalone-bootstrap.yml
    target_platform: linux
    category: platform
    app: rhbk_standalone

  - src: rhbk/ado-manage-client-bootstrap.yml
    dest: playbooks/rhbk/ado-manage-client-bootstrap.yml
    target_platform: openshift
    category: platform
    app: rhbk_client

  - src: rhbk/ado-manage-entraid-idp.yml
    dest: playbooks/rhbk/ado-manage-entraid-idp.yml
    target_platform: openshift
    category: platform
    app: rhbk_idp

  - src: rhbk/ado-manage-realm-bootstrap.yml
    dest: playbooks/rhbk/ado-manage-realm-bootstrap.yml
    target_platform: openshift
    category: platform
    app: rhbk_realm

  - src: rhbk/ado-realm-bootstrap.yml
    dest: playbooks/rhbk/ado-realm-bootstrap.yml
    target_platform: openshift
    category: platform
    app: rhbk_federation

  - src: rhbk/ado-setup-mapper-bootstrap.yml
    dest: playbooks/rhbk/ado-setup-mapper-bootstrap.yml
    target_platform: openshift
    category: platform
    app: rhbk_mapper

  - src: rhel/ado-patch-host-bootstrap.yml
    dest: playbooks/rhel/ado-patch-host-bootstrap.yml
    target_platform: linux
    category: patching
    app: rhel

  - src: rhel/ado-compliance-bootstrap.yml
    dest: playbooks/rhel/ado-compliance-bootstrap.yml
    target_platform: linux
    category: compliance
    app: compliance

  - src: rhel/ado-stig-hardening-bootstrap.yml
    dest: playbooks/rhel/ado-stig-hardening-bootstrap.yml
    target_platform: linux
    category: compliance
    app: stig

  - src: satellite/ado-install-satellite-bootstrap.yml
    dest: playbooks/satellite/ado-install-satellite-bootstrap.yml
    target_platform: linux
    category: satellite
    app: satellite_server_install

  - src: satellite/ado-configure-satellite-bootstrap.yml
    dest: playbooks/satellite/ado-configure-satellite-bootstrap.yml
    target_platform: linux
    category: satellite
    app: satellite_server_install

  - src: satellite/ado-manage-content-view-bootstrap.yml
    dest: playbooks/satellite/ado-manage-content-view-bootstrap.yml
    target_platform: linux
    category: satellite
    app: satellite_content_view

  - src: satellite/ado-register-to-satellite-bootstrap.yml
    dest: playbooks/satellite/ado-register-to-satellite-bootstrap.yml
    target_platform: linux
    category: patching
    app: satellite_client_tools

  - src: satellite/ado-configure-satellite-oidc-bootstrap.yml
    dest: playbooks/satellite/ado-configure-satellite-oidc-bootstrap.yml
    target_platform: linux
    category: satellite
    app: satellite_oidc
