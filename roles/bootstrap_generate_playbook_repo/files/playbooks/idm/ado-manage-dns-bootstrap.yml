---
- name: Manage IDM DNS
  hosts: ipaserver
  gather_facts: false
  tasks:
    - name: Manage IDM DNS records
      ansible.builtin.debug:
        msg: "TODO: Add IDM DNS management tasks/role here"
