# SPDX-License-Identifier: GPL-3.0-or-later-0
---
# defaults file for mount

utilities_mount_fstype_detection: "{{ mount_fstype_detection | default(false) }}" # Set to true to enable filesystem type detection
