---
# argument spec file for ado.aap_build_ee

argument_specs:
  main:
    short_description: Build a custom Ansible execution environment image.
    description: |
      This role builds an Ansible execution environment (EE) image from a caller-selected
      base EE image and caller-provided collection requirements.
    options:
      aap_build_ee_base_ee:
        type: str
        description: Base EE image name and tag, for example ee-minimal-rhel9:latest.
        required: true
      aap_build_ee_source_image_repository:
        type: str
        description: Source container repository path used to pull the base EE image.
        required: true
      aap_build_ee_collections:
        type: dict
        description: >-
          Mapping of collection name to version constraint used when rendering
          requirements.yml.
        default: {}
      aap_build_ee_collection_files:
        type: list
        elements: str
        description: List of local collection artifact paths (*.tar.gz) to include in the built EE image.
        default: []
      aap_build_ee_output_image:
        type: str
        description: Target image name and tag for the built EE image.
        default: custom-ee:latest
      aap_build_ee_build_context:
        type: str
        description: Directory path used as ansible-builder context.
        default: /tmp/aap_build_ee
      aap_build_ee_builder_executable:
        type: str
        description: ansible-builder executable name or full path.
        default: ansible-builder
      aap_build_ee_package_manager_path:
        type: str
        description: >-
          Optional package manager executable path override used by ansible-builder in the
          base image. When empty, ansible-builder uses its own default.
        default: ""
      aap_build_ee_ansible_core:
        type: str
        description: >-
          Optional ansible-core version constraint installed via ansible-builder
          dependencies. Required for minimal base images that do not include Ansible.
        default: ""
      aap_build_ee_ansible_runner:
        type: str
        description: >-
          Optional ansible-runner version constraint installed via ansible-builder
          dependencies. Required for minimal base images that do not include Ansible Runner.
        default: ""
